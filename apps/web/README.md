# @yagura/web

The Yagura web app (Next.js App Router + Tailwind): landing page, wallet-connect
dashboard, public `/name/[fqn]` status pages, the `/renew/[name]` one-tap renewal
deep-link, and the public `/metrics` dashboard.

**Status: placeholder.** Built in Phase 4.
