{
  "name": "@yagura/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Yagura's poller: a single-shot script run on a schedule by GitHub Actions. Refreshes tracked name state, enqueues due alerts, and drains the delivery queue, then exits.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "poll": "pnpm --filter @yagura/core build && pnpm --filter @yagura/bot build && tsx src/poll-once.ts",
    "ops": "tsx src/ops.ts"
  },
  "dependencies": {
    "@yagura/bot": "workspace:*",
    "@yagura/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "grammy": "^1.45.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
